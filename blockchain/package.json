{
  "name": "the-ledger-blockchain",
  "version": "1.0.0",
  "description": "Solidity smart contracts for The Ledger e-voting prototype",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "the-ledger-blockchain-evoting": "file:.."
  }
}
